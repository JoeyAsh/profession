import {Box} from '@mui/material';
import {TestButton} from '@profession/common';

export const App = (): JSX.Element => {
    return (
        <Box>
            <TestButton />
        </Box>
    );
};
